CREATE SCHEMA "MULTIPLEDS" AUTHORIZATION "D2INST1";

CREATE TABLE "MULTIPLEDS"."PROFILES"  (
		  "ID_PROFILE" VARCHAR(50 OCTETS) NOT NULL , 
		  "PROFILE_NAME" VARCHAR(50 OCTETS) NOT NULL );
		  
ALTER TABLE "MULTIPLEDS"."PROFILES" 
	ADD CONSTRAINT "PROFILES_PK" PRIMARY KEY
		("ID_PROFILE")
	ENFORCED ;