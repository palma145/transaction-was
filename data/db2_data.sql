CREATE SCHEMA "MULTIPLEDS" AUTHORIZATION "D2INST1";

CREATE TABLE "MULTIPLEDS"."USERS"  (
		  "USERNAME" VARCHAR(50 OCTETS) NOT NULL , 
		  "PASSWORD" VARCHAR(50 OCTETS) NOT NULL );
		 
ALTER TABLE "MULTIPLEDS"."USERS" 
	ADD CONSTRAINT "USERS_PK" PRIMARY KEY
		("USERNAME")
	ENFORCED ;